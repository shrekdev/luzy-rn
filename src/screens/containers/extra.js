import {connect} from "react-redux";
import {Extra} from "../components/extra";

const mapStateToProps = (state) => {
    const {safeArea} = state.appReducer;
    return {
        safeArea,
    };
};

export default connect(mapStateToProps, null)(Extra);